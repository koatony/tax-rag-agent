API_BASE_URL=http://127.0.0.1:8000
# API_KEY is optional and not currently required by the live API (no auth today,
# see api_contract.md). Left here only in case a future deployment puts an
# authenticating reverse proxy in front of it -- if set, run_api_test.py will
# send it as an X-API-Key header.
API_KEY=
